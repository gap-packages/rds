<Chapter Label="Some functions for everyday use">
<Heading>Some functions for everyday use</Heading>
This chapter contains a number of functions that did not fit in
anywhere else. Some of them might be useful for other people, too, so
they were included here.
<Section Label="Groups and actions">
<Heading>Groups and actions</Heading>
<#Include Label="OnSubgroups">
<P/>
<#Include Label="RepsCClassesGivenOrder">
</Section>
<Section Label="Iterators">
<Heading>Iterators</Heading>
<#Include Label="CartesianIterator">
<P/>
<#Include Label="ConcatenationOfIterators">
</Section>
<Section Label="Lists and Matrices">
<Heading>Lists and Matrices</Heading>
<#Include Label="IsListOfIntegers">
<P/>
<#Include Label="List2Tuples">
<P/>
<#Include Label="MatTimesTransMat">
<P/>
<#Include Label="PartitionByFunctionNF">
<P/>
<#Include Label="PartitionByFunction">
<Log><![CDATA[
gap> PartitionByFunctionNF([-1..5],x->x^2);
[ [ 0 ], [ -1, 1 ], [ 2 ], [ 3 ], [ 4 ], [ 5 ] ]
gap> test:=function(x) 
> if x>0 then return Sqrt(x);
>  else return fail;
> fi;
> end;
function( x ) ... end
gap> PartitionByFunction([-1..5],test);  
[ [ 1 ], [ 4 ], [ 5 ], [ 2 ], [ 3 ] ]
gap> SetInfoLevel(InfoRDS,2);
gap> PartitionByFunction([-1..5],test);  
#I  -2-
[ [ 1 ], [ 4 ], [ 5 ], [ 2 ], [ 3 ] ]
gap> PartitionByFunctionNF([-1..5],test);
Error, function returned <fail> called from
...
brk> return;
[ [ 1 ], [ 4 ], [ 5 ], [ 2 ], [ 3 ], [ -1, 0 ] ]
]]></Log>
</Section>
<Section Label="Cyclotomic numbers">
<Heading>Cyclotomic numbers</Heading>
<#Include Label="IsRootOfUnity">
<P/>
<#Include Label="CoeffList2CyclotomicList">
<P/>
<#Include Label="AbssquareInCyclotomics">
<P/>
<#Include Label="CycsGivenCoeffSum">
</Section>
<Section Label="Filters and Categories">
<Heading>Filters and Categories</Heading>
The following was originally posted at the &GAP; forum by Thomas
Breuer <Cite Key="BreuersAnswer"/>.
<P/>
Each filter in &GAP; is either a simple filter or a meet of filters.
For example, <C>IsInt</C> and <C>IsPosRat</C> are simple filters,
and <C>IsPosInt</C> is defined as their meet <C>IsInt and IsPosRat</C>.
<P/>
Each <E>simple filter</E> is of one of the following kinds.
<P/>
1. property:
   Such a filter is an operation, the filter value can be computed.
   The (unary) methods of this operation must return <K>true</K> or <K>false</K>,
   and the return value is stored in the argument,
   except if the argument is of a basic data type such as cyclotomic
   (including rationals and integers), finite field element, permutation,
   or internally represented list --the latter with a few exceptions.
   Examples of properties are <C>IsFinite</C>, <C>IsAbelian</C>, <C>IsSSortedList</C>.
<P/>
2. attribute tester:
   Such a filter is associated to an operation that has been created
   via <C>DeclareAttribute</C>,
   in the sense that the value is <K>true</K> if and only if a return value
   for (a unary method of) this operation is stored in the argument.
   Currently, attribute values are stored in objects in the filter
   <C>IsAttributeStoringRep</C>.
   Examples of attribute testers are <C>HasSize</C>, <C>HasCentre</C>,
   <C>HasDerivedSubgroup</C>.
<P/>
2.' property tester:
   Such a filter is similar to an attribute tester,
   but the associated operation is a property.
   So property testers can return <K>true</K> also if the argument is not in
   the filter <C>IsAttributeStoringRep</C>.
   Examples of property testers are <C>HasIsFinite</C>, <C>HasIsAbelian</C>,
   <C>HasIsSSortedList</C>.
<P/>
3. category or representation:
   These filters are not associated to operations, their values cannot
   be computed but are set upon creation of an object and should not be
   changed later, such that for a filter of this kind, one can rely on
   the fact that if the value is <K>true</K> then it was <K>true</K> already when
   the object in question was created.
<P/>
The distinction between representation and category is intended to
   express dependency on or independence of the way how the object is
   stored internally.
   For example, <C>IsPositionalObjectRep</C>, <C>IsComponentObjectRep</C>, and
   <C>IsInternalRep</C> are filters of the representation kind;
   the idea is that such filters are used in low level methods,
   and that higher level methods can be implemented without referring
   to these filters.
<P/>
Examples of categories are <C>IsInt</C>, <C>IsRat</C>, <C>IsPerm</C>, <C>IsFFE</C>,
   and filters expressing algebraic structures,
   such as <C>IsMagma</C>, <C>IsMagmaWithOne</C>, <C>IsAdditiveMagma</C>.
   When one calls such a filter, one can be sure that no computation is
   triggered.
   For example, whenever a quotient of two integers is formed, the result
   is clearly in the filter <C>IsRat</C>, but the system also stores the value
   of <C>IsInt</C>, i.e., &GAP; does not support <Q>unevaluated rationals</Q> for
   which the <C>IsInt</C> value is computed on demand and then stored.
<P/>
4. other filters:
   Some filters do not belong to the above kinds,
   they are not associated to operations but they are intended to be
   set (or even reset) by the user or by functions also after the creation
   of objects.
   Examples are <C>IsQuickPositionList</C>, <C>CanEasilyTestMembership</C>,
   <C>IsHandledByNiceBasis</C>.
<P/>
Each <E>meet of filters</E> can involve computable simple filters (properties,
attribute and property testers) and not computable simple filters
(categories, representations, other filters).
When one calls a meet of two filters then the two filters from which
the meet was formed are evaluated (if necessary).
So a meet of filters is computable only if at least one computable simple
filter is involved.
<P/>
<#Include Label="IsComputableFilter">
<P/>
'IsComputableFilter(<A>filter</A>)' returns <A>true</A> if a the filter <A>filter</A>
is computable. Filters for which 'IsComputableFilter' returns <A>false</A>
may be used in 'DeclareOperation'.
<Example><![CDATA[
gap> IsComputableFilter( IsFinite );
true
gap> IsComputableFilter( HasSize );
true
gap> IsComputableFilter( HasIsFinite );
true
gap> IsComputableFilter( IsPositionalObjectRep );
false
gap> IsComputableFilter( IsInt );
false
gap> IsComputableFilter( IsQuickPositionList );
false
gap> IsComputableFilter( IsInt and IsPosRat );
false
gap> IsComputableFilter( IsMagma );
false
]]></Example>
</Section>
</Chapter>
