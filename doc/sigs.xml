<Chapter Label="Invariants for Difference Sets">
<Heading>Invariants for Difference Sets</Heading>
This chapter contains an important tool for the generation of
difference sets. It is called the <Q>coset signature</Q> and is an
invariant for equivalence of partial relative difference sets.  For
large <M>\lambda</M>, there is an invariant calculated by
<Ref Func="MultiplicityInvariantLargeLambda"/>. This invariant can be used
complementary to the coset signature and is explained in section
<Ref Sect="An invariant for large lambda"/>.
<P/>
Most of the methods explained here are not commonly used. If you do
not want to know how coset signatures work in detail, you can safely
skip a large part of this and go straight to the explanation of
<Ref Func="SignatureDataForNormalSubgroups"/> and <Ref Func="ReducedStartsets"/>.
<P/>
The functions <Ref Func="RDSFactorGroupData"/>, <Ref Func="MatchingFGData"/> will be
interesting for you, if you look for difference sets with the same
parameters in different groups. <Ref Func="SignatureDataForNormalSubgroups"/> and
<Ref Func="SigInvariant"/>
<P/>
The last section (<Ref Sect="Blackbox functions"/>) of this chapter has some
functions which allow the user to use coset signatures with even less
effort. But be aware that these functions make choices for you that
you probably do not want if you do very involved calculations. In
particular, the coset signatures are not stored globally and hence
cannot be reused. For a demonstration of these easy-to-use functions,
see chapter <Ref Chap="A basic example"/>
<Section Label="The Coset Signature">
<Heading>The Coset Signature</Heading>
Let <M>R \subseteq G</M> be a (partial) relative difference set (for
definition see <Ref Sect="Introduction"/>) with forbidden set <M>N\subseteq G</M>. Let
<M>U\leq G</M> be a normal subgroup and <M>C=\{g_1,\dots, g_{|G:U|}\}</M> be a
system of representatives of <M>G/U</M>.
<P/>
The intersection number of <M>R</M> with <M>Ug_i</M> is defined as <M>v_i=|R\cap
Ug_i|</M>. For every normal subgroup <M>U\leq G</M> the multiset <M>\{|R\cap
Ug_i| \colon g_i\in C\}</M> is called <Q>coset signature of <M>R</M> (relative
to <M>U</M>)</Q>.
<P/>
Let <M>D\subseteq G</M> be a relative difference set and
<M>\{v_1,\dots,v_{|G:U|}\}</M> its coset signature. Then the following
equations hold (see <Cite Key="Bruck55"/>,<Cite Key="RoederDiss"/>):
<P/>
<Display>
\matrix{
\sum v_i=k\\
\sum v_i^2=\lambda(|U|-|U\cap N|)+k\\
\sum_j v_j v_{ij}=
	 \lambda(|U|-|g_iU \cap N|)&amp;{ for }   g_i\not\in U}
</Display>
where <M>v_{ij}=|D\cap g_ig_jU|</M>.  If the forbidden set <M>N</M> is a
subgroup of <M>G</M> we have <M>|g_iU\cap N|</M> is either <M>0</M> or equal to
<M>|U\cap N|</M>.
<P/>
Given a group <M>G</M>, the forbidden set <M>N\subseteq G</M> and some normal
subgroup <M>U\leq G</M>, the right sides of this equations are known. So we
may ask for tuples <M>(v_1,\dots,v_{|G:U|})</M> solving this system of
equations. Of course, we index the <M>v_i</M> with the elements of <M>G/U</M>,
so the last equation poses conditions to the ordering of the tuple
<M>(v_1,\dots,v_{|G:U|})</M>.
<P/>
So we call any multiset <M>\{v_1,\dots,v_{|G:U|}\}</M> solving the above
equations an <Q>admissible signature</Q> for <M>U</M>.
<P/>
<#Include Label="CosetSignatureOfSet">
<P/>
<#Include Label="CosetSignatures">
<P/>
<#Include Label="TestSignatureLargeIndex">
<P/>
<#Include Label="TestSignatureCyclicFactorGroup">
<P/>
<#Include Label="TestedSignatures">
<P/>
<#Include Label="TestedSignaturesRelative">
<P/>
<#Include Label="SigInvariant">
<P/>
For an example using <Ref Func="SignatureDataForNormalSubgroups"/> see the example
after <Ref Func="ReducedStartsets"/> below.
<P/>
<#Include Label="SignatureDataForNormalSubgroups">
<P/>
The following three functions are used by
<Ref Func="SignatureDataForNormalSubgroups"/>. If you do not want to write your
own function for signature management, you might not need them.
<P/>
<#Include Label="RDSFactorGroupData">
<P/>
<#Include Label="MatchingFGDataNonGrp">
<P/>
<#Include Label="MatchingFGData">
<P/>
<#Include Label="ReducedStartsets">
<#Include Label="RDS_MaxAutsizeForOrbitCalculation">
</Section>
<Section Label="An invariant for large lambda">
<Heading>An invariant for large lambda</Heading>
<#Include Label="MultiplicityInvariantLargeLambda">
(Read this output as: two elements occur once and two occur twice).
<P/>
This invariant can be used for <Ref Func="ReducedStartsets"/> complementary to the
signature invariant by defining
<Log><![CDATA[
gap> partfunc:=function(list)
> local sig;                                           
> if sig=fail
> then return fail;
> fi;
> return [MultiplicityInvariantLargeLambda(list,Gdata),SigInvariant(list,sigdata)];
> end;
function( list ) ... end
]]></Log>
<A>partfunc</A> can then be passed to <Ref Func="ReducedStartsets"/>. Of course,
<A>sigdata</A> has to be the list of records defining the coset signatures.
</Section>
<Section Label="Blackbox functions">
<Heading>Blackbox functions</Heading>
Here are a few functions used in chapter <Ref Chap="A basic example"/>. These
are meant as black boxes for quick tests. Some of them make choices
for you which might not be suitable to the chase you consider, so for
serious studies, consider using the more complicated-looking functions
above (an example for this comprises chapter <Ref Chap="An Example Program"/>).
<P/>
<#Include Label="SignatureData">
<#Include Label="NormalSgsHavingAtMostNSigs">
<#Include Label="SuitableAutomorphismsForReduction">
<#Include Label="StartsetsInCoset">
</Section>
</Chapter>
