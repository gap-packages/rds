<Chapter Label="An Example Program">
<Heading>An Example Program</Heading>
Here is a similar example to that in chapter <Ref Chap="A basic example"/>. But
now we do a little more handwork to see how things work.  Now we will
calculate the relative difference sets of <Q>Dembowski-Piper type d</Q>
and order <M>16</M>. These difference sets represent projective planes
which admit a quasiregular collineation group such that the fixed
structure is an anti-flag. See <Cite Key="DembowskiPiper"/>, <Cite Key="Dembowski"/>
or <Cite Key="RoederDiss"/> for details.
<P/>
To have a little more output, you may want to increase <Ref Func="InfoRDS"/>:
<Log><![CDATA[
gap> SetInfoLevel(InfoRDS,3);
]]></Log>
First, define some parameters and calculate the data needed:
<Example><![CDATA[
gap> k:=16;;lambda:=1;;groupOrder:=255;; #Diffset parameters
gap> forbiddenGroupOrder:=15;;
gap> maxtest:=10^6;;                     #Bound for sig testing
gap> G:=CyclicGroup(groupOrder);
<pc group of size 255 with 3 generators>
gap> Gdata:=PermutationRepForDiffsetCalculations(G);;
gap> MakeImmutable(Gdata);; 
]]></Example>
Now the forbidden group is calculated in a very ineffective way. Then
we calculate admissible signatures. As there are only few normal
subgroups in <M>G</M>, we calculate them all. For other groups, one should
choose more wisely.
<Example><![CDATA[
gap> N:=First(NormalSubgroups(Gdata.G),i->Size(i)=forbiddenGroupOrder);
Group([ f1*f3^9, f2*f3^10 ])
gap> globalSigData:=[];;
gap> normals:=Filtered(NormalSubgroups(Gdata.G),n->Size(n) in [2..groupOrder-1]);; 
gap> sigdat:=SignatureDataForNormalSubgroups(normals,globalSigData,
>                             N,Gdata,[k,lambda,maxtest,true]);;
]]></Example>
The last step gives better results, if a larger <A>maxtest</A> is chosen.
But it also takes more time. To find a suitable <A>maxtest</A>, the output
of <Ref Func="SignatureDataForNormalSubgroups"/> can be used, if
<C>InfoLevel(InfoRDS)</C> is at least <M>2</M>. Note that for recalculating
signatures, you will have to reset <A>globalSigData</A> to <C>[]</C>. Try experimenting
with <A>maxtest</A> to see the effect of signatures for the generation of
startsets.
<P/>
Now examine the signatures found. Look if there is a normal subgroup
which has only one admissible signature (of course, you can also use
<Ref Func="NormalSgsHavingAtMostNSigs"/> here):
<Example><![CDATA[
gap> Set(Filtered(sigdat,s->Size(s.sigs)=1 and Size(s.sigs[1])<=5),i->i.sigs);
[ [ [ 0, 4, 4, 4, 4 ] ], [ [ 4, 4, 8 ] ] ]
]]></Example>
Great! we'll take the subgroup of index <M>3</M>. The cosets modulo this
group will be used to generate startsets and we assume that the
trivial coset contains <M>8</M> elements of the difference set.
So we generate startsets in <M>U</M> and make a first reduction. For this,
we need <M>U</M> and <M>N</M> as lists of integers (recall that difference sets are
asumed to be lists of integers). We will call these lists <M>Up</M> and
<M>Np</M>.
Furthermore, we will have to choose a suitable group of automorphisms
for reduction. As <M>G</M> is cyclic, we may take <M>Gdata.Aac</M> here. A good
choice is the stabilizer of all cosets modulo <M>U</M>. Yet sometimes
larger groups may be possible. For example if we want to generate
start sets in <M>U</M> and then do a brute force search. In this case, we
may take the stabilizer of <M>U</M> for reduction.
<Log><![CDATA[
gap> U:=First(sigdat,s->s.sigs=[ [ 4, 4, 8 ] ]).subgroup; 
Group([ f2, f3 ])
gap> cosets:=RightCosets(G,U);
gap> U1:=cosets[2];;U2:=cosets[3];;
gap> Up:=GroupList2PermList(Set(U),Gdata);;  
gap> Np:=GroupList2PermList(Set(N),Gdata); 
[ 1, 12, 25, 43, 78, 97, 115, 116, 134, 151, 169, 188, 207, 238, 249 ]
gap> comps:=Difference(Up,Np);; 
gap> ssets:=List(comps,i->[i]);;
gap> ssets:=ReducedStartsets(ssets,[Gdata.Aac],sigdat,Gdata.diffTable); 
#I  Size 80
#I  2/ 0 @ 0:00:00.061
[ [ 3 ], [ 4 ] ]
]]></Log>
Observe that <M>1</M> is assumed to be element of every difference set and
is not recorded explicitly. So the partial difference sets represented
by <M>ssets</M> at this point are [ [ 1, 3 ], [ 1, 4 ] ]. Now the
startsets are extended to size <M>7</M> using elements of <M>Up</M>. The runtime
varies depending on the output of <Ref Func="SignatureDataForNormalSubgroups"/>
and hence on <M>maxtest</M>.
<Log><![CDATA[
gap> repeat 
>     ssets:=ExtendedStartsets(ssets,comps,Np,7,Gdata); 
>     ssets:=ReducedStartsets(ssets,[Gdata.Aac],sigdat,Gdata.diffTable);; 
> until ssets=[] or Size(ssets[1])=7; 
#I  Size 133
#I  3/ 0 @ 0:00:00.133
#I  Size 847
#I  4/ 0 @ 0:00:00.949
#I  Size 6309
#I  4/ 0 @ 0:00:07.692
#I  Size 21527
#I  5/ 0 @ 0:00:28.337
#I  Size 15884
#I  4/ 0 @ 0:00:21.837
#I  Size 1216
#I  4/ 0 @ 0:00:01.758
gap> Size(ssets);
192
]]></Log>
At a higher level of <Ref Func="InfoRDS"/>, the number of start sets which are
discarded because of wrong signatures are also shown.
Now the cosets are changed. Here we use the <C>NoSort</C> version of
<Ref Func="ExtendedStartsets"/>.  This leads to a lot of start sets in the first
step. If the number of start sets in <M>U</M> is very large, this could be
too much for a reduction. Then the only option is using the brute
force method. But also for the brute force search,
<Ref Func="ExtendedStartsetsNoSort"/> must be called first (remember that we chos
an enumeration of <M>G</M> and assume the last element from each startset
to be the largeset <Q>interesting</Q> one for further completions).
<Log><![CDATA[
gap> comps:=Difference(GroupList2PermList(Set(U1),Gdata),Np);;
gap> ssets:=ExtendedStartsetsNoSort(ssets,comps,Np,11,Gdata);;
gap> ssets:=ReducedStartsets(ssets,[Gdata.Aac],sigdat,Gdata.diffTable);; 
#I  Size 8640
#I  9/ 0 @ 0:00:14.159
gap> Size(ssets);  
6899
]]></Log>
And as above, we continue:
<Log><![CDATA[
repeat 
    ssets:=ExtendedStartsets(ssets,comps,Np,11,Gdata); 
    ssets:=ReducedStartsets(ssets,[Gdata.Aac],sigdat,Gdata.diffTable);; 
until ssets=[] or Size(ssets[1])=11; 
comps:=Difference(GroupList2PermList(Set(U2),Gdata),Np); 
RaiseStartSetLevelNoSort(ssets,comps,Np,15,Gdata); 
repeat 
    ssets:=ExtendedStartsets(ssets,comps,Np,15,Gdata); 
    ssets:=ReducedStartsets(ssets,[Gdata.Aac],sigdat,Gdata.diffTable);; 
until ssets=[] or Size(ssets[1])=15; 
]]></Log>
</Chapter>
